import { KBDisplayPipe } from './kb-display.pipe';

describe('KBDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new KBDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
